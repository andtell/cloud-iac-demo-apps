apiVersion: apps/v1
kind: Deployment
metadata:
  name: cadec-demo
spec:
  selector:
    matchLabels:
      app: cadec-demo
  replicas: 2
  template:
    metadata:
      labels:
        app: cadec-demo
    spec:
      containers:
      - name: cadec-demo
        image: ghcr.io/andtell/vote-app:aea67b0e
        env:
        - name : NODE_ENV
          value : "dev"
        - name : EXPRESS_PORT
          value : "8080"
        - name : VERSION
          value : "1.2.1"
        ports:
        - containerPort: 8080
